{
  "$schema": "https://schema.railpack.com",
  "steps": {
    "install": {
      "commands": [
        "corepack enable",
        "corepack prepare yarn@1.22.22 --activate",
        "cd .. && YARN_PRODUCTION=false yarn install --frozen-lockfile"
      ]
    },
    "build": {
      "commands": ["cd .. && yarn workspace npo-client-template build"]
    }
  },
  "deploy": {
    "startCommand": "yarn preview:railway"
  }
}
