{
  "$schema": "https://schema.railpack.com",
  "steps": {
    "install": {
      "commands": [
        "corepack enable",
        "yarn install"
      ]
    },
    "build": {
      "commands": ["yarn build"]
    }
  },
  "deploy": {
    "startCommand": "yarn preview:railway"
  }
}